---
name: Bug report
about: Something isn't working as expected
labels: bug
---

**What happened**
<!-- One-line summary, then details. -->

**Steps to reproduce**
1.
2.

**Expected vs. actual**

**Environment**
- OS / Windows build:
- PowerShell version (`$PSVersionTable.PSVersion`):
- AutoHotkey v2 version:
- ASR backend: local FunASR+Qwen3 / sherpa-onnx CPU / cloud (Volcano / Tencent / Xfyun)
- Install mode: installed (`install.bat`) / portable (`start.bat`) / dev (`install.ps1`)

**Logs**
<!-- Attach relevant lines from %LOCALAPPDATA%\voice-stt\ (stream-debug.log, ptt-debug.log).
     Redact any tokens, hostnames, or personal paths first. -->
